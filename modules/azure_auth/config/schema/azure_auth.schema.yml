# Schema for the configuration files of the azure_auth module.
azure_auth.settings:
  type: config_object
  label: 'azure_auth settings'
  mapping:
    example:
      type: string
      label: 'Example'
